// aula 25 - operação ternária
// ? : - ele é uma forma de simplificar o if/else

const pontuacaoUser = 1000;          // true ?          false :  
const nivelUser = pontuacaoUser >= 1000 ? 'Usuário VIP' : 'Usuário normal';

console.log(nivelUser);